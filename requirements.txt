# Core dependencies
# Versions are pinned to the set currently in use (captured 2026-06) so a future
# package release cannot silently break a known-good Speculate environment.
# Bump these deliberately after testing rather than letting them float.
# Note: openmpi is provided as a system/conda package (not pip-managed), so it
# is intentionally left unpinned here.
marimo==0.23.9
uvicorn[standard]==0.49.0
openmpi

# Add other dependencies below
# HuggingFace integration
huggingface_hub==1.19.0

# Data processing
pandas==2.3.3
numpy==2.2.6
pyarrow==22.0.0
scipy==1.16.3
flatdict==4.1.0
toml==0.10.2

# Visualization
altair<6
vegafusion==2.0.3
vl-convert-python==1.8.0

# Speculate Core
emcee==3.1.6
corner==2.2.3
astropy==7.2.0
extinction==0.4.8
sigfig==1.3.19
cuml-cu12; sys_platform == 'linux'
torch
torchvision

# Optional optimizers
cma==4.4.4
optuna==4.8.0
scikit-learn==1.8.0

# Debugging and Profiling
pyinstrument==5.1.1

# Arviz for MCMC diagnostics (last to avoid potential conflicts with other dependencies)
arviz==0.23.0
tabulate==0.9.0

# Versioning
python-semantic-release==10.5.3